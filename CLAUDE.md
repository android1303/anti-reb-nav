# Anti-Reb Nav

@docs/MASTER_PLAN.md

## Правила роботи з кодом

- Гіроскоп expo-sensors віддає рад/с; ядро працює в град/с.
- Константи калібрування (YAW_SIGN, ZUPT_*, *_SCALE_FACTOR) змінювати
  лише за результатами аналізу CSV з полігону.
- Логіку ядра в telemetry.js (машина станів, ZUPT, DR) не змінювати без
  підтвердження відтворенням реального заїзду.
- Збірка APK: GitHub Actions (assembleRelease), не EAS.
- Після змін в ядрі оновлювати розділ "Поточний стан" у MASTER_PLAN.md.

## Поточний стан ядра: telemetry.js v16

За результатами польового тесту (Вишгород, 1.38 км):

- Скасовано детекцію нерухомості по accel/gyro (v15): вона плутала рух
  з постійною швидкістю зі стоянкою. STOPPED/ZUPT — лише при свіжому OBD
  зі швидкістю 0; розрив OBD = стан OBD_LOST.
- Екстраполяція швидкості: OBD 2–5 с без даних -> остання відома
  швидкість, понад 5 с -> 0.
- ZUPT: bias оцінюється лише на стоянках >= 10 с (після 1 с
  «заспокоєння»), зважено усереднюється між стоянками; поріг розкиду
  2.5 °/с (вібрація двигуна).
- Паузи потоку JS 1–5 с доінтегровуються для позиції (колонка CSV `gapS`).
- Синхронізація з Firestore вимкнена під час запису (блокувала потік JS),
  запускається після stopSession; syncNow повертає reason 'recording'.
- Під час запису екран не гасне (expo-keep-awake).

@AGENTS.md
